# 🔞乐成漫画

## 基本信息
- **书源名称**: 🔞乐成漫画
- **书源地址**: https://www.lcmhx.asia
- **书源分组**: 🔞漫画
- **书源类型**: 图片 (2)
- **创建时间**: 2026-02-18 10:21:36
- **来源**: /yuedu/shuyuan/content/id/6949.html
- **发布时间**: 1天前 
- **下载次数**: 未知

## 书源描述
1天前 

## 规则配置

### 搜索规则
```js
{
  "bookList": ".search-result a",
  "bookUrl": "a@href",
  "coverUrl": "img@src",
  "name": "a@title"
}
```

### 发现规则
```js
{
  "bookList": ".elevation-3",
  "bookUrl": "a@href",
  "coverUrl": "img@src",
  "kind": ".v-chip__content@text",
  "name": ".hv-title@text"
}
```

### 详情规则
```js
{
  "intro": "[style=\"    font-size: 14px;font-weight: 300;\"] span@text##简介\\s*:\\s*",
  "name": "[style=\"    font-size: 24px;font-weight: 300;color: #f3c669;\"] span@text",
  "tocUrl": "@js:\nbaseUrl.replace(/\\/$/, \"-1/\")"
}
```

### 目录规则
```js
{
  "chapterList": "@js:\nconst getUrl = (p) => baseUrl.replace(/\\d+\\/$/, `${p}/`);\nlet max = 200; // 取一个大数作为最后一页id\nlet lastUrl = getUrl(max);\nconst J = org.jsoup.Jsoup;\nlet doc = J.parse(result);\nconst list = [];\nlet listEl = doc.select(\".listmh li a\")\nif(listEl.size()){\n\t let lastDoc = J.parse(java.ajax(lastUrl))\n\t let lastEl = lastDoc.select(\".listmh li a\").last()\n\t if(lastEl){\n\t   max = String(lastEl.attr(\"href\")).match(/-(\\d+)\\/$/)?.[1] || 1;\n\t   for(let i = 1; i <= max; i++){\n\t     let url = getUrl(i);\n\t     list.push({ title: `第${i}章`, url })\n\t   }\n\t \t}\n}else{\n\t list.push({title: \"立即欣赏\"})\n}\n\nlist",
  "chapterName": "title",
  "chapterUrl": "url"
}
```

### 正文规则
```js
{
  "content": "main.content img@html",
  "title": ".pagination__item--active.0@text"
}
```

## 完整书源JSON
```js
{
  "bookSourceComment": "原作者：wen-jun-in\n\n1. 临时修复目录不全问题；\n2. 搜索存在简繁问题，书名有简有繁，关键字全简体或全繁体均会搜索失败，网站问题，非书源能解决。by~26/2/17",
  "bookSourceGroup": "🔞漫画",
  "bookSourceName": "🔞乐成漫画",
  "bookSourceType": 2,
  "bookSourceUrl": "https://www.lcmhx.asia",
  "customButton": false,
  "customOrder": 2,
  "enabled": true,
  "enabledCookieJar": true,
  "enabledExplore": true,
  "eventListener": false,
  "exploreUrl": "韩漫::/mctype/1-{{page}}\n单行本::/mctype/2-{{page}}\n同人志::/mctype/3-{{page}}\nCosplay::/mctype/4-{{page}}\n最新::/latest/{{page}}",
  "header": "{\"User-Agent\": \"Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36\"}",
  "lastUpdateTime": "1771260167887",
  "respondTime": 180000,
  "ruleBookInfo": {
    "intro": "[style=\"    font-size: 14px;font-weight: 300;\"] span@text##简介\\s*:\\s*",
    "name": "[style=\"    font-size: 24px;font-weight: 300;color: #f3c669;\"] span@text",
    "tocUrl": "@js:\nbaseUrl.replace(/\\/$/, \"-1/\")"
  },
  "ruleContent": {
    "content": "main.content img@html",
    "title": ".pagination__item--active.0@text"
  },
  "ruleExplore": {
    "bookList": ".elevation-3",
    "bookUrl": "a@href",
    "coverUrl": "img@src",
    "kind": ".v-chip__content@text",
    "name": ".hv-title@text"
  },
  "ruleSearch": {
    "bookList": ".search-result a",
    "bookUrl": "a@href",
    "coverUrl": "img@src",
    "name": "a@title"
  },
  "ruleToc": {
    "chapterList": "@js:\nconst getUrl = (p) => baseUrl.replace(/\\d+\\/$/, `${p}/`);\nlet max = 200; // 取一个大数作为最后一页id\nlet lastUrl = getUrl(max);\nconst J = org.jsoup.Jsoup;\nlet doc = J.parse(result);\nconst list = [];\nlet listEl = doc.select(\".listmh li a\")\nif(listEl.size()){\n\t let lastDoc = J.parse(java.ajax(lastUrl))\n\t let lastEl = lastDoc.select(\".listmh li a\").last()\n\t if(lastEl){\n\t   max = String(lastEl.attr(\"href\")).match(/-(\\d+)\\/$/)?.[1] || 1;\n\t   for(let i = 1; i <= max; i++){\n\t     let url = getUrl(i);\n\t     list.push({ title: `第${i}章`, url })\n\t   }\n\t \t}\n}else{\n\t list.push({title: \"立即欣赏\"})\n}\n\nlist",
    "chapterName": "title",
    "chapterUrl": "url"
  },
  "searchUrl": "/mcsearch/-------/?wd={{key}}",
  "weight": 0
}
```

## 学习要点

### 1. 选择器规则分析
- **搜索列表**: .search-result a
- **章节列表**: @js:
const getUrl = (p) => baseUrl.replace(/\d+\/$/, `${p}/`);
let max = 200; // 取一个大数作为最后一页id
let lastUrl = getUrl(max);
const J = org.jsoup.Jsoup;
let doc = J.parse(result);
const list = [];
let listEl = doc.select(".listmh li a")
if(listEl.size()){
	 let lastDoc = J.parse(java.ajax(lastUrl))
	 let lastEl = lastDoc.select(".listmh li a").last()
	 if(lastEl){
	   max = String(lastEl.attr("href")).match(/-(\d+)\/$/)?.[1] || 1;
	   for(let i = 1; i <= max; i++){
	     let url = getUrl(i);
	     list.push({ title: `第${i}章`, url })
	   }
	 	}
}else{
	 list.push({title: "立即欣赏"})
}

list
- **正文内容**: main.content img@html

### 2. 规则类型
- **CSS选择器**: 使用 `@text`、`@ownText`、`@html` 等提取类型
- **XPath规则**: 使用 `xpath:` 前缀
- **JSONPath规则**: 使用 `$` 符号
- **正则表达式**: 使用 `##pattern##replacement` 格式
- **JavaScript规则**: 使用 `@js:` 或 `java.` 前缀

### 3. 特殊配置
- **登录功能**: 否
- **Cookie管理**: 是
- **并发控制**: 未配置
- **自定义请求头**: 是

### 4. 性能优化
- **响应时间**: 180 秒
- **智能权重**: 0

## 适用场景
- 小说阅读
- 漫画浏览
- 有声书收听
- 其他资源获取

## 标签
#书源 #阅读 #Legado #图片

---
*本文档由参考学习模块自动生成*

# 茶杯狐

## 基本信息
- **书源名称**: 茶杯狐
- **书源地址**: https://www.cupfox7.com/
- **书源分组**: 未分类
- **书源类型**: 文本 (0)
- **创建时间**: 2026-02-18 10:32:55
- **来源**: /yuedu/shuyuan/content/id/6832.html
- **发布时间**: 01/04 04:09
- **下载次数**: 未知

## 书源描述


## 规则配置

### 搜索规则
```js
{
  "bookList": "class.stui-vodlist[0]@tag.li",
  "bookUrl": "tag.a@href",
  "checkKeyWord": "魔王",
  "coverUrl": "tag.a@data-original",
  "kind": "tag.a@tag.span@text",
  "name": "tag.a@title"
}
```

### 发现规则
```js
[]
```

### 详情规则
```js
{
  "intro": "class.stui-content__detail@p@text##详情$##"
}
```

### 目录规则
```js
{
  "chapterList": "id.playlist1@tag.ul@tag.li",
  "chapterName": "tag.a@text",
  "chapterUrl": "tag.a@href"
}
```

### 正文规则
```js
{
  "content": "@js:\nvar pUrl = result.match(/\"url_next\":\"([^\"]*)\"/)\npUrl = pUrl[1].replace(/\\\\/g, \"\");\nif (cache.get(book.name+title)){\n    try{\n        java.startBrowserAwait(pUrl, title);\n    }catch(err){\n       \"其实...1+1=2!\"\n    }\n}else{\n    cache.put(book.name+title,true, 31536000);\n}\n\"请刷新查看内容\";"
}
```

## 完整书源JSON
```js
{
  "bookSourceName": "茶杯狐",
  "bookSourceType": 0,
  "bookSourceUrl": "https://www.cupfox7.com/",
  "customOrder": 0,
  "enabled": true,
  "enabledCookieJar": true,
  "enabledExplore": true,
  "exploreUrl": "国漫::vodshow/guochandongman/page/{{page}}.html\n日韩::vodshow/rihandongman/page/{{page}}.html\n欧美::vodshow/oumeidongman/page/{{page}}.html",
  "lastUpdateTime": "1767470986002",
  "respondTime": 180000,
  "ruleBookInfo": {
    "intro": "class.stui-content__detail@p@text##详情$##"
  },
  "ruleContent": {
    "content": "@js:\nvar pUrl = result.match(/\"url_next\":\"([^\"]*)\"/)\npUrl = pUrl[1].replace(/\\\\/g, \"\");\nif (cache.get(book.name+title)){\n    try{\n        java.startBrowserAwait(pUrl, title);\n    }catch(err){\n       \"其实...1+1=2!\"\n    }\n}else{\n    cache.put(book.name+title,true, 31536000);\n}\n\"请刷新查看内容\";"
  },
  "ruleExplore": [],
  "ruleSearch": {
    "bookList": "class.stui-vodlist[0]@tag.li",
    "bookUrl": "tag.a@href",
    "checkKeyWord": "魔王",
    "coverUrl": "tag.a@data-original",
    "kind": "tag.a@tag.span@text",
    "name": "tag.a@title"
  },
  "ruleToc": {
    "chapterList": "id.playlist1@tag.ul@tag.li",
    "chapterName": "tag.a@text",
    "chapterUrl": "tag.a@href"
  },
  "searchUrl": "vodsearch/{{key}}----------{{page}}---.html",
  "weight": 0
}
```

## 学习要点

### 1. 选择器规则分析
- **搜索列表**: class.stui-vodlist[0]@tag.li
- **章节列表**: id.playlist1@tag.ul@tag.li
- **正文内容**: @js:
var pUrl = result.match(/"url_next":"([^"]*)"/)
pUrl = pUrl[1].replace(/\\/g, "");
if (cache.get(book.name+title)){
    try{
        java.startBrowserAwait(pUrl, title);
    }catch(err){
       "其实...1+1=2!"
    }
}else{
    cache.put(book.name+title,true, 31536000);
}
"请刷新查看内容";

### 2. 规则类型
- **CSS选择器**: 使用 `@text`、`@ownText`、`@html` 等提取类型
- **XPath规则**: 使用 `xpath:` 前缀
- **JSONPath规则**: 使用 `$` 符号
- **正则表达式**: 使用 `##pattern##replacement` 格式
- **JavaScript规则**: 使用 `@js:` 或 `java.` 前缀

### 3. 特殊配置
- **登录功能**: 否
- **Cookie管理**: 是
- **并发控制**: 未配置
- **自定义请求头**: 否

### 4. 性能优化
- **响应时间**: 180 秒
- **智能权重**: 0

## 适用场景
- 小说阅读
- 漫画浏览
- 有声书收听
- 其他资源获取

## 标签
#书源 #阅读 #Legado #文本

---
*本文档由参考学习模块自动生成*
